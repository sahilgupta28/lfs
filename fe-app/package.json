{
    "name": "placeholder-for-fe-app",
    "scripts": {
        "dev": "vite",
        "start": "vite --host --port 3000",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^4.0"
    }
}
